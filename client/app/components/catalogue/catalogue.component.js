import template from './catalogue.html';
import controller from './catalogue.controller';
import './catalogue.scss';

const catalogueComponent = {
  restrict: 'E',
  template,
  controller
};

export default catalogueComponent;
