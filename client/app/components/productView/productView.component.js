import template from './productView.html';
import controller from './productView.controller';
import './productView.scss';

const productViewComponent = {
  restrict: 'E',
  template,
  controller
};

export default productViewComponent;
